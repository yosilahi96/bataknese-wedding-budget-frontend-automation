BASE_FE_URL=https://bataknese-wedding-budget.vercel.app
PROJECT_DETAIL_PATH=
BROWSER=chromium
HEADLESS=false
DEFAULT_TIMEOUT=60000
VIEWPORT_WIDTH=1440
VIEWPORT_HEIGHT=900
SLOW_MO=0
IGNORE_HTTPS_ERRORS=false
SCREENSHOT=only-on-failure
VIDEO=off
TRACE=retain-on-failure

# Optional CI / env fallback when config/credentials_*.json is not present.
# Prefer local JSON files for day-to-day runs (see config/*.example.json).
# LOGIN_VALID_EMAIL=
# LOGIN_VALID_PASSWORD=
# LOGIN_INVALID_EMAIL=invalid@example.com
# LOGIN_INVALID_PASSWORD=invalid-password
